#pragma once
#include <stdio.h>
#include <stdlib.h>

void calculate_integral_os(sys*);

double** generate_overlap_1d(int, int, double, double, double, double, double);

double overlap();

double Sab(bfn, bfn);
